# Mergify configuration for auto-merge after prow-github-actions adds labels
pull_request_rules:
  - name: Auto merge with lgtm label
    conditions:
      - "label=lgtm"
      - "check-success~=.*"
      - "-draft"
    actions:
      merge:
        method: squash